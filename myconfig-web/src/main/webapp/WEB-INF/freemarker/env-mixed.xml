<?xml version="1.0" encoding="UTF-8" ?>
<configuration id="${model.id?xml}" application="${model.name?xml}" environment="${model.environment?xml}" version="${model.version?xml}">
	<#list model.values as value>
		<param name="${value.key?xml}">${value.value?xml}</param>
	</#list>
</configuration>